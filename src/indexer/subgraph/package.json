{
  "name": "netero-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://127.0.0.1:8020 netero/subgraph",
    "deploy-local": "graph deploy --node http://127.0.0.1:8020 --ipfs http://127.0.0.1:5001 netero/subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.66.2",
    "@graphprotocol/graph-ts": "^0.33.0"
  }
}
